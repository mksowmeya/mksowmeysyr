version: 2

models:
  - name: obt_sales
    description: The sales fact table combined with all of its outrigger dimensions into one big table. Transaction fact table with transaction details of customers, products, and employees within a specific date interval.
